
	<div class="row">
		<p class="sub-title"><strong>検索結果</strong></p></br>
		<table>
		<tr>
			<th><%= sort_link(@search, :title, "並び替える") %></th>
		</tr>
		</table></div></br>
		<% @search_recipes.each do |recipe| %>
		<div class="col-md-3">
		<table>
			<p class="search-recipe-text"> <%= recipe.title %></p>
			<%= link_to recipe_path(recipe.id) do %>
				<p> <%= attachment_image_tag recipe, :image, format: 'jpg', fallback: '/images/noimage.jpg', size: '150x150', class: "recipe-image" %></p>
			<% end %>
		</table>
		</div>
		<% end %>
	</div>

